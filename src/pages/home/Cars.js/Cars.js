import React from 'react';
import { useParams } from 'react-router-dom';

const Cars = () => {
    const { cartCategory } = useParams()

    return (
        <div>

        </div>
    );
};

export default Cars;